spring:
  application:
    name: veterinary-management
  datasource:
    url: jdbc:postgresql://localhost:5432/veterinary-management
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true